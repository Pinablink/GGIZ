import { GICompDirective } from './g-icomp.directive';

describe('GICompDirective', () => {
  it('should create an instance', () => {
    const directive = new GICompDirective();
    expect(directive).toBeTruthy();
  });
});
