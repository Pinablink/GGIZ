import { NgShowComandDirective } from './ng-show-comand.directive';

describe('NgShowComandDirective', () => {
  it('should create an instance', () => {
    const directive = new NgShowComandDirective();
    expect(directive).toBeTruthy();
  });
});
