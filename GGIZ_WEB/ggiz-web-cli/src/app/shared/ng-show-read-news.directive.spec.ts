import { NgShowReadNewsDirective } from './ng-show-read-news.directive';

describe('NgShowReadNewsDirective', () => {
  it('should create an instance', () => {
    const directive = new NgShowReadNewsDirective();
    expect(directive).toBeTruthy();
  });
});
